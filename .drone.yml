pipeline:
  build:
    image: docker
    commands:
      - docker build -f Dockerfile-builder -t waddle.vapor-builder .
      - docker push vapor-builder
      - docker rmi vapor-builder
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    privileged: true

